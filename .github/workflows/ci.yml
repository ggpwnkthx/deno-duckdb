name: CI
on:
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: denoland/setup-deno@v1
        with:
          deno-version: 2.x
          cache: true

      - name: CI
        run: deno task ci
